import * as React from 'react';
import { Button, Header1 } from '@cebus/react-components';
import { AppContainer } from '../components';
// import { makeStyles } from '@griffel/react';

const Home = () => {
  return (
    <AppContainer>
      <Header1>This is a header</Header1>
      <Button>Test</Button>
    </AppContainer>
  );
};

export default Home;
